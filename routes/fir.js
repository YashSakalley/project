var express = require('express');
var router = express.Router();

/*
router.get('/fir', (req, res) => {
    var auth = req.flash('auth');
    console.log(auth);
    if(auth == 'yes')
      res.render('fir');
    else
      res.redirect('/');
  });

  router.get('/logout', (req, res) => {
    req.flash('auth', 'no');
    res.redirect('/');
  });
*/



module.exports = router;
